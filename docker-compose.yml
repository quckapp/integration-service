version: '3.8'

services:
  integration-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quckapp-integration-service
    ports:
      - "5016:5016"
    environment:
      - PORT=5016
      - DATABASE_URL=mysql+asyncmy://quckapp:quckapp@mysql:3306/quckapp_integrations
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-your-32-byte-encryption-key-here!}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
      - JIRA_CLIENT_ID=${JIRA_CLIENT_ID:-}
      - JIRA_CLIENT_SECRET=${JIRA_CLIENT_SECRET:-}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
    depends_on:
      - mysql
      - redis
    networks:
      - quckapp-network
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    container_name: quckapp-integration-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=quckapp_integrations
      - MYSQL_USER=quckapp
      - MYSQL_PASSWORD=quckapp
    ports:
      - "3318:3306"
    volumes:
      - integration-mysql-data:/var/lib/mysql
    networks:
      - quckapp-network

  redis:
    image: redis:7-alpine
    container_name: quckapp-integration-redis
    ports:
      - "6390:6379"
    networks:
      - quckapp-network

networks:
  quckapp-network:
    driver: bridge

volumes:
  integration-mysql-data:
